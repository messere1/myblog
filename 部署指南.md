# 墨笺博客 · Supabase 后端 + Vercel 部署指南

整个过程分四大步：① 建 Supabase 后端 → ② 替换代码 → ③ 本地跑通 → ④ Vercel 上线。
全程免费，预计 30~40 分钟。

═══════════════════════════════════════════════
## 第一步：建 Supabase 后端
═══════════════════════════════════════════════

1. 打开 https://supabase.com → 用 GitHub 登录 → New Project
   - 起个名字（如 moblog），设一个数据库密码（记下来），区域选离你近的（如 Singapore）
   - 等 1~2 分钟项目初始化完成

2. 建表：左侧 SQL Editor → New query → 把 `supabase/setup.sql` 全部内容粘进去 → Run
   - 这会创建 posts / categories 表、开启权限策略，并导入你原来的 6 篇示例文章

3. 建后台登录账号：左侧 Authentication → Users → Add user → Create new user
   - 填邮箱 + 密码（这就是你登录后台用的账号），勾选 "Auto Confirm User"
   - ⚠️ 这一步很重要：不建用户的话登录会失败

4. 拿配置：左侧 Settings → API，记下两个值：
   - Project URL（形如 https://xxxxx.supabase.co）
   - anon public key（一长串）

═══════════════════════════════════════════════
## 第二步：替换代码
═══════════════════════════════════════════════

代码已经替换完成！你只需要安装依赖：

    npm install

═══════════════════════════════════════════════
## 第三步：本地跑通
═══════════════════════════════════════════════

1. 项目根目录建一个 `.env` 文件（参考 `.env.example`），填入第一步拿到的值：

    VITE_SUPABASE_URL=https://xxxxx.supabase.co
    VITE_SUPABASE_ANON_KEY=你的-anon-public-key

2. 启动（注意：现在不再需要 json-server，直接 vite 即可）：

    npm run dev

   你原来的 npm run dev 里带了 concurrently 启动 mock。
   现在后端是 Supabase，不需要 mock 了，可以手动改 package.json 的 dev：
       "dev": "vite"
   不改也能跑，只是会顺带启动一个用不到的 mock，无害。

3. 验证：
   - 首页能看到 6 篇文章（来自 Supabase）→ 后端通了
   - /admin/login 用第一步建的邮箱密码登录 → 进后台
   - 后台新建一篇文章 → 保存 → 回首页能看到 → 发文章通了！

═══════════════════════════════════════════════
## 第四步：Vercel 上线
═══════════════════════════════════════════════

1. 先把代码 push 到 GitHub（.env 不要提交，.gitignore 里已忽略）

2. 打开 https://vercel.com → 用 GitHub 登录 → Add New → Project
   → 选你的 myblog 仓库 → Import

3. ⭐ 关键：配置环境变量（否则线上连不上 Supabase）
   在 Import 页面展开 Environment Variables，加两条：
       VITE_SUPABASE_URL = https://xxxxx.supabase.co
       VITE_SUPABASE_ANON_KEY = 你的-anon-public-key

4. Build 设置（一般自动识别，确认一下）：
   - Build Command: npm run build
   - Output Directory: dist
   （你仓库已有 vercel.json，通常无需额外改）

5. Deploy → 等 1~2 分钟 → 得到一个 xxx.vercel.app 网址

6. 上线后回头改两个地方：
   - `scripts/gen-rss.mjs` 里 SITE.link 改成你的 vercel 网址
   - 如果用了 giscus 评论，去 giscus 配置里把域名加上

═══════════════════════════════════════════════
## 改了什么（技术说明）
═══════════════════════════════════════════════

- 后端从 json-server 换成 Supabase（Postgres + 自动 REST API + Auth）
- api/posts.ts、api/categories.ts 内部改用 supabase 客户端，但**函数签名没变**
- 所以 store、组件、路由、布局全部不用动
- 登录改用 Supabase Auth，权限策略设成"所有人可读、登录才能写"
- 数据库用 snake_case，api 层做了 camelCase 映射，前端无感
- RSS 改成构建时从 Supabase 拉最新文章

═══════════════════════════════════════════════
## 两个最容易踩的坑
═══════════════════════════════════════════════

1. **一定要在 Supabase 建登录用户**（Authentication → Users），否则登录失败
   ——因为不像原来 mock 里写死了 admin 账号

2. **Vercel 一定要配那两个环境变量**，否则线上页面连不上数据库
